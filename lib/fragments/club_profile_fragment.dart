import 'package:flutter/material.dart';

class ClubProfileFragment extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
            "This is the location of the widget that gonna curry the info of the "
            "club, it might be a separated widget"),
      ),
    );
  }
}
